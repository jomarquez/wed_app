<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <%= csrf_meta_tag %>  
	<%= render 'layouts/stylesheets' %>
  </head>
  <body>
    <div class="container">
      <%= render 'layouts/header' %>
      <div class="span-17 section round"> 
        <%= yield %>
      </div>  
	  <%= render 'layouts/footer' %>
    </div>
  </body>
</html>